# Copyright © 2012 Martin Ueding <dev@martin-ueding.de>

SHELL=/bin/bash

# Backup.
@weekly       nice backup-webservers

# Cleaning.
@reboot       nice rm -fv "$HOME/.lesshst"
@reboot       nice rm -fv "$HOME/.mysql_history"
@reboot       nice rm -fv "$HOME/.q3a/qkey"
@monthly      nice find "$HOME" -name "octave-core" -print -delete
@monthly      nice find "$HOME" -name ".unison.*" -print -delete

# Virus scan.
0 18 1 */3 *  nice clamscan -r "$HOME" &> "$HOME/TODO/Virus Scan $(date --rfc-3339=seconds).log"
